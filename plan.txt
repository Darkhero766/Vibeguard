VIBESANE / VIBEGUARD — PROJECT PLAN & CHANGE LOG
================================================
Purpose
-------
Single source of truth for major project actions, decisions, repository changes, deployments, environment changes, bugs, fixes, and next steps. This file MUST be updated after every major offline or online action and every meaningful commit. Before major work, read this file first.

PROJECT BASELINE
----------------
- Product: VibeSane / VibeGuard developer-security platform.
- Core workflow: GitHub repository connection, security scanning, findings, security health, repository protection, and Pro functionality.
- Hosting context: frontend on Vercel; backend/API on Render.
- GitHub integration is active and supports repository access/workflows.
- Pro supports multiple protected repositories; current target is up to 5 repositories.
- Payment provider: Dodo Payments.

CHANGE LOG
----------
2026-09-09 — Multi-repository Review & Fix CTA
- Problem: Review & Fix Findings CTA could disappear when the selected protected repository had persisted finding counts but was not the current lastScan record.
- Root cause: CTA rendering was gated on selectedScan instead of the selected repository's available finding counts.
- Action: Updated artifacts/vibeguard/src/components/SecurityDashboard.tsx so the Review & Fix Findings CTA is driven by total findings and remains usable for a selected protected repository even when detailed scan data is not the current lastScan.
- Also added safer fallback behavior for opening the latest available security report.
- Repository: Darkhero766/Vibeguard.
- Important follow-up: FindingDetail currently needs the remembered/desired “Get Prompt” workflow restored if that UI is still expected.

2026-09-09 — Dodo Payments production activation
- Existing Dodo Payments integration was inspected before changing configuration.
- Backend expects these environment variables: DODO_PAYMENTS_API_KEY, DODO_PAYMENTS_ENVIRONMENT, DODO_PRO_PRODUCT_ID, DODO_WEBHOOK_SECRET.
- Backend mode logic is exact/case-sensitive: test_mode uses the test API; every other value uses the live API.
- Production mode value must therefore be exactly live_mode.
- DODO_PRO_PRODUCT_ID must contain the live Dodo Pro product ID (pdt_...).
- DODO_PAYMENTS_API_KEY must contain the LIVE API key, not a product ID.
- User updated Render environment variables to activate live mode.
- Do NOT record or expose secret API key/webhook-secret values in this file.

2026-09-09 — Dodo live checkout authentication failure
- First real production checkout attempt reached the backend successfully but Dodo returned HTTP 401 during checkout creation.
- Root cause class: Dodo rejected the backend's payment API credentials for the selected live environment.
- Action: Verify LIVE DODO_PAYMENTS_API_KEY, exact live_mode, matching live product ID, and redeploy.
- No secret values recorded here.

2026-09-09 — SEO / Google indexing investigation
- Google Search Console reported “Page with redirect” for pages on the VibeSane site.
- Existing SEO setup was reviewed: robots.txt, sitemap.xml, homepage canonical, and index/follow directives were already present.
- Conclusion: “Page with redirect” means the redirecting URL itself is excluded; it does not by itself mean the VibeSane homepage is blocked from indexing.
- Main remaining concern: brand indexing/ranking and whether old VibeSane URLs/results are outranking the current site.

2026-09-06 — Public-domain / network issue
- Public-domain page was reported as not opening on both iPad and another device on the same Wi-Fi.
- Likely network/DNS/router-level issue rather than device-specific; keep as separate infrastructure investigation unless revisited.

2026-09-09 — Dodo live-mode $0 test product created
- User created an additional Dodo product for a controlled live-mode checkout test without charging.
- Live test product ID: pdt_0NnCwrdKUXghcYu8rP3SN.
- Result: $0 live checkout succeeded. Live Dodo authentication, checkout-session creation, and hosted checkout flow are confirmed working.
- Next: restore the paid live Pro product ID in Render before production sales.

2026-09-09 — Purchased Pro pricing state
- Product decision: users who already have an active Pro entitlement should not be shown the normal Pro purchase CTA as if they need to buy Pro again.
- Action: Updated artifacts/vibeguard/src/pages/PricingPage.tsx to read authoritative Pro state from AuthContext usage.plan.
- Code commit: ba74bb1c3630ef6476fddf4ae2735ee65082be82.
- Deployment status: code committed; Vercel verification pending.

2026-09-09 — Pro commit/PR live security alert UX
- Pro users should receive an immediate in-app alert when a protected repository's new commit or pull request introduces security findings.
- Existing backend already handles GitHub push/pull_request events, delta scans, GitHub Check Runs, and protection events.
- Updated artifacts/vibeguard/src/components/ProtectionActivity.tsx with new-event detection and VibeSane Security Alert UI containing repository, SHA, findings, severity counts, and Review & Fix Findings action.
- Code commit: 1c82fb208443030566957041ce44ceadabc870ed.
- Deployment status: production verification pending.

2026-09-10 — Referral/Affiliate program intentionally paused
- Existing referral/affiliate UI is preserved: artifacts/vibeguard/src/components/AffiliateWelcomePopup.tsx and artifacts/vibeguard/src/pages/AffiliatePage.tsx remain in the repository.
- Added a local REFERRALS_ENABLED feature flag set to false so the new-account referral popup is not displayed and referral logic does not run.
- Referral page/assets remain preserved. Full referral tracking/reward activation is deferred until approximately the first 100 customers.
- Code commit: ac399c2784f3d174d5f46afb34c93ecc94e6cdc7.
- Deployment status: frontend verification pending.

2026-09-10 — Pro split scan quota: protected repositories + public URL scans
- Pro has 10 total monthly manual scan credits, split into two independent buckets: 5 protected-repository setup scans and 5 public-repository URL scans.
- Protected setup scans are consumed only by creating a new protected repository; updating/reprotecting an existing protected repository does not consume another setup credit.
- Public URL scans consume only the public bucket and remain available after all 5 protected-repository slots are used.
- Both buckets reset together on the monthly reset cycle; aggregate scans_used/scans_limit remain compatible with existing UI.
- Backend split quota logic exists in artifacts/api-server/src/lib/plan.ts with consumeProtectedScan/consumePublicScan and separate counters.
- Code commits: 35d804392b8fd7ee974acbdd756bc71a1a59a2ed, 6ef3df61e2831ba0669992db6cf077f2eca1e3ad, 1458b50c0f3ff705fd8c72b04e284e833f408037.
- Deployment status: backend deployment/end-to-end verification pending.

2026-09-10 — Dedicated public repository scan UX
- Bug confirmed: the Protect Repository panel still exposed a “Paste URL” tab that submitted the URL to /api/protection. Once 5 protected repositories existed, this incorrectly showed “Repository limit reached (5)” even though the user still had 5 separate public URL scan credits.
- Product decision: public URL scanning must be a separate workflow from repository protection.
- Added artifacts/vibeguard/src/pages/PublicScanPage.tsx at /scan-public. It accepts a public GitHub URL, calls the standalone /api/scans endpoint, shows a dedicated loading/scan state with the repository URL and progress details, then shows the temporary result with file/finding/severity details.
- Public scan results are intentionally ephemeral: no localStorage report persistence, no protected_repository record, no GitHub event protection, and no permanent repository history. The only persisted effect is the required server-side public scan quota consumption.
- The result page explicitly states that the scan is temporary and not saved to repository history. User can scan another public repo after a completed result.
- Updated artifacts/vibeguard/src/App.tsx to route /scan-public to PublicScanPage.
- Updated artifacts/vibeguard/src/components/ProtectRepositoryPanel.tsx to remove the Paste URL protection path. The protection modal now uses My repos for protection and provides a dedicated “Scan public repository” link to /scan-public.
- This guarantees that having 5 protected repositories does not block the separate public scan allowance.
- Code commits: 59bcbe09b38c9e76aa90d12465be6e37d3015e55 (new page), 9ebd440889331dc04990f4d5c317953e5dd77250 (route), 9a7749404c531468bea2d028158c1596f732afae (protection panel).
- Deployment status: Vercel deployment and live end-to-end UI/quota verification pending.

2026-09-10 — Restore Paste URL / My repos tab UX and launch public scan page
- User feedback: the two-tab scan UI (Paste URL / My repos) must remain in the main security-center flow. The Paste URL tab is the public one-time scan entry point; My repos remains the protected-repository flow.
- Action: preserved the existing ScanForm tab UI in artifacts/vibeguard/src/AppOriginal.tsx and added a flow bridge in artifacts/vibeguard/src/App.tsx so submitting a valid Paste URL navigates to /scan-public instead of consuming the protected-repository path.
- The /scan-public page now auto-starts the pasted URL scan after navigation, giving the requested dedicated page/loading animation and repository details before the temporary result.
- The public scan allowance display now falls back to 5 for Pro when the usage payload does not yet expose public_scans_limit, preventing the incorrect “0 of 0 public scans” display while preserving server-side enforcement.
- Protected repository protection remains separate; the My repos tab continues to be the protected workflow.
- Code commits: 004758570a555b401a6ed5a3bf30d8ad04adf2b5 (tab-to-public-scan flow bridge), eeae48750d9cba19b87b8d84dd5f4cb17da34ff3 (public quota display fallback).
- Deployment status: Vercel deployment and mobile verification pending.

2026-09-10 — Restore two-tab UX in Protect Repository panel
- User reported that the deployed/mobile Protect Repository panel still showed the newer “Different job, different quota” public-scan card instead of the original two-tab interface.
- Root cause: artifacts/vibeguard/src/components/ProtectRepositoryPanel.tsx had been changed to a single My repos flow with a separate public-scan card, which did not preserve the requested Paste URL / My repos tab presentation.
- Action: restored the two-tab UI directly in ProtectRepositoryPanel.tsx: “Paste URL” and “My repos”. Paste URL accepts a public GitHub URL and navigates to /scan-public with the URL; My repos remains the protected-repository picker and /api/protection flow.
- Public URL scanning remains separate from protection and does not create/save a protected repository. The dedicated /scan-public page remains responsible for the scanning animation and temporary results.
- Code commit: 37bd5f74110faeee82c33d03dfb27a5b1a24497f.
- Deployment status: Vercel deployment and mobile verification pending.

2026-09-10 — Public scan repeat-scan 404 + stale quota counter fix
- User reproduced two production issues: after repeated public scans the /scan-public flow could fall into the app's 404/not-found route until refresh, and the “public scans remaining” counter did not decrease immediately after a successful scan.
- Root cause for the counter: /api/scans correctly consumes the server-side public bucket only after a successful scan, but PublicScanPage did not refresh AuthContext usage afterward. The frontend therefore displayed the stale pre-scan usage row.
- Root cause/risk for repeat-scan navigation: App.tsx used a second effect in PublicScanFlowBridge to simulate a click on the dedicated scan page's form after navigation. That introduced timing/routing races on repeated navigation. The scan page now owns query-driven auto-start after the authenticated session is ready, and the bridge only performs the main Paste URL -> /scan-public navigation.
- Action: extended UsageRow with protected/public quota fields; PublicScanPage now refreshes authoritative usage immediately after successful scans; query URL scanning now passes the query URL directly to runScan; removed the simulated form-click effect from App.tsx; “Scan another public repo” also clears the query URL before another scan.
- Code commits: 7316aa7884ef25bed985423628a9dac141807041 (UsageRow quota fields), 08bf7df53879fd91d72435eb573d2da5aaa5b83b (quota refresh/repeat-flow hardening), 213785b4c0ee41d8b48bb305c49095122aa39b28 (query URL passed directly to scan), 8b3bd203fdf49250090c1fa8fdf7ae07c545b63e (remove route-bridge auto-click race).
- Deployment status: code committed; Vercel/Render deployment and live mobile verification required before marking production fixed.

WORKING RULES
-------------
1. Every meaningful code commit MUST be recorded here with date, purpose, files, and commit SHA when available.
2. Every major online change (Render/Vercel settings, production environment, payment configuration, domains, webhooks, GitHub settings) MUST be recorded here.
3. Every major offline change/decision must also be recorded here, even if no commit is created.
4. Never store secrets, API keys, webhook secrets, passwords, tokens, or private credentials in this file.
5. Before making major changes, read this file and preserve existing architecture/decisions unless intentionally changing them.
6. When a change affects production, record deployment/verification status separately from code-complete status.
7. Referral UI may remain in the repository while the referral program is disabled until approximately 100 customers; do not remove the existing page/assets when pausing it.
8. Pro manual scan quota is permanently defined as 5 protected-repository setup scans + 5 public GitHub URL scans per monthly cycle unless intentionally changed and recorded here.
9. Public URL scanning must never call the repository-protection endpoint or create a protected repository. It is a temporary scan workflow and uses only the public scan bucket.
10. The main security-center ScanForm must retain the two-tab UX: Paste URL and My repos. Paste URL launches the temporary public scan flow; My repos is for protected repositories.
11. Do not replace the two-tab Paste URL / My repos UX with a separate public-scan card unless intentionally approved and recorded first.
12. PublicScanPage owns the lifecycle of /scan-public query-driven scans; do not add simulated button-click effects in the global route bridge.
13. After every successful public scan, the frontend MUST refresh authoritative usage so the public quota counter changes immediately.

CURRENT NEXT STEPS
------------------
1. Restore the paid LIVE Pro product ID in Render after the successful $0 test.
2. Keep DODO_PAYMENTS_ENVIRONMENT exactly live_mode.
3. Verify DODO_PAYMENTS_API_KEY remains the LIVE Dodo API key; do not share the secret value.
4. Verify the paid Pro checkout and confirm checkout -> webhook/sync -> database -> Pro entitlement.
5. Confirm failed-payment behavior and subscription renewal/cancellation handling.
6. Verify the new Pricing page Pro-active state after Vercel deployment.
7. Verify the new commit/PR Security Alert by pushing a controlled test change to a protected repo and confirming the alert + Review & Fix action.
8. Restore/verify the “Get Prompt” workflow in Finding Detail if it is required by the current product UX.
9. Continue Google Search Console indexing investigation after production site changes.
10. Keep referral/affiliate acquisition paused until approximately 100 customers; preserve the UI and reactivate the mechanics only when intentionally decided.
11. Verify Pro quota end-to-end: add 5 protected repositories and confirm protected setup quota reaches 5/5 while public URL scanning remains available at 0/5; then perform 5 public URL scans and confirm the public bucket reaches 5/5 and the aggregate reaches 10/10.
12. Deploy and verify the public-scan repeat-flow fix on mobile: run at least 3 consecutive public scans, confirm no 404/not-found transition, confirm the dedicated loading animation appears each time, and confirm the public counter decreases after every successful scan.
13. Verify the restored two-tab Protect Repository panel on mobile after Vercel deployment: Paste URL tab must accept a public GitHub URL and launch /scan-public; My repos tab must remain the protected flow.
